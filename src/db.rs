use crate::config::Config;
use crate::util::error::TinyError;

pub struct DB {}

// pub fn open_db(config: Config) -> DB {}


impl DB {
    // pub fn new() -> DB {}
    // pub fn write(&self, key: Vec<&u8>) -> Result<(), TinyError> {}
    // pub fn get(&self, key: Vec<&u8>) -> Option<Vec<&8>> {}
}